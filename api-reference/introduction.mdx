# API Reference

Welcome to the Superlog API. Use the endpoints below to interact with the platform programmatically.

## Base URL

```text
https://api.superlog.com
```

## Authentication

All requests require a bearer token in the `Authorization` header:

```bash
Authorization: Bearer <your-api-key>
```